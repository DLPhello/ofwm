# Design reference

- **Source:** Live site at oatleywines.com.au (structure + imagery only — not a layout clone).
- **Direction:** Warm paper background, wine accent (`--accent`), serif headlines, uppercase micro-labels, dark footer, full-bleed hero rotation, dense `object-contain` brand grid for logo legibility.
- **Screenshots:** Optional — capture hero + portfolio grid + news list in browser for future polish.
